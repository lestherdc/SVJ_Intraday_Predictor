Para este projecto se trabajo con la version Python 3.14.2, tenderemos problemas en algunas librerias por lo que se implementaran una que otra funcion de las librerias que no podremos usar por problemas de versiones.
Paquetes necesarios
  -numpy
  -scipy
  -pandas
  -yfinance
  -matplotlib
